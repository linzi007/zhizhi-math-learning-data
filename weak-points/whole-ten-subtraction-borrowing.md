# 薄弱项：整十/整百减两位数借位

## 当前状态

- 状态：薄弱项
- 首次发现日期：2026-05-17
- 最近证据日期：2026-05-18
- 复发次数：1
- 典型表现：`50-32=28`、`50-42=18`、`70-32=48`、`90-45=55`、`60-24=46`、`40-18=32`，错误都表现为答案比正确答案大10；`100-64` 独立不会，讲解后能做。家长观察：基本不能直接得出两位数答案，而是先算个位写个位，再算十位补十位；所有整十减两位数都忘记带小帽子。

## 诊断判断

这不是单纯“100以内减法不会”，而是一个更窄的借位链条问题：

- `整十减个位数` 能做，如 `60-7`、`50-9`、`80-6`、`100-8`。
- `整十减两位数` 容易错，因为个位借10后，十位忘记少1。
- `100减两位数` 需要把100看成10个十，再借1个十给个位，链条更长，所以独立不稳。

## 可能原因

- `借位链条不完整`：只完成“个位借10”，没有同步完成“十位少1”。
- `小帽子迁移不足`：普通退位题提醒后能用小帽子，但整十/整百题没有主动用。
- `步骤顺序不稳`：先急着写个位答案，再回头算十位；但没有在写个位前先标出十位少1。
- `速度压过步骤`：这类题做得很快，但快在错误路径上。
- `数位意识不稳`：借出去的1个十已经变成个位的10，十位不能仍按原数计算。

## 讲解策略

固定一句关键话：

```text
借出去的那个十，已经到个位了，十位必须少1。
```

整十减两位数固定三步：

```text
1. 个位0不够减，先借1个十。
2. 先给十位戴小帽子，提醒十位少1。
3. 再算个位，最后算十位。
```

`100减两位数` 固定说法：

```text
100先看成10个十。
借1个十给个位后，还剩9个十。
再算个位和十位。
```

## 验证练习策略

- 不做40题大卷，先做8-12题短练。
- 每题只要是整十/整百减两位数，就必须写小帽子或小竖式。
- 训练顺序先不追求直接口算出完整答案，允许“个位、十位分步写”，但必须先标小帽子再写个位。
- 批改时重点看是否“答案大10”。如果大10，直接标记 `十位忘记少1`。
- 混入少量 `整十减个位数` 做对比：会 `70-8` 不代表会 `70-38`。

## 复发处理策略

- 如果普通退位正确、整十减两位数错，保持本薄弱项，不扩大为“全部退位减法不会”。
- 如果连续两次专项能 90% 以上正确，再转为 `待巩固`，并放入应用题中验证迁移。
- 如果应用题里再次出现 `50-32=28` 这类错误，标记为 `迁移失败`。

## 历史记录

| 日期 | 来源 | 结果 | 判断 | 下一步 |
| --- | --- | --- | --- | --- |
| 2026-05-17 | 学校卷同类变式巩固卷 | 应用题中 `50-32=28` | 迁移失败 | 单独练应用题里的整十退位 |
| 2026-05-17 | 多步应用题和排队问题小练 | 第14题 `50-42=18` | 复发 | 两位数减法横式必须写小帽子或小竖式 |
| 2026-05-18 | 每日口算小练：100以内减法 | 35/40，用时14分钟；`70-32`、`90-45`、`60-24`、`40-18` 全部大10；`100-64` 需讲解；家长观察为都忘记带帽子 | 薄弱项 | 8-12题整十/整百减两位数专项 |

## 下次动作

- 生成或口头做8-12题：`30-12`、`50-21`、`70-43`、`80-56`、`90-27`、`100-18`、`100-45`、`100-73`。
- 每题执行顺序必须是：先判断个位够不够，先写小帽子，再写个位答案，最后写十位答案。
- 如果没有小帽子，即使答对也记录为“过程未稳”。
